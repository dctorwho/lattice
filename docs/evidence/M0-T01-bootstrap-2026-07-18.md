# M0-T01 bootstrap evidence

Recorded at: 2026-07-18T12:48:31.4029836Z

## Final environment

- Node.js: `v24.18.0`
- Corepack: `0.35.0`
- `corepack pnpm --version`: `11.12.0` (exit 0)
- `pnpm.cmd --version`: `11.12.0` (exit 0)
- PowerShell bare `pnpm` is not used for evidence because the local execution policy blocks the generated `pnpm.ps1`; `pnpm.cmd` and `corepack pnpm` resolve the approved shim without changing system policy.

## Dependency installation

- `corepack pnpm install`: final run exit 0; installed 84 packages from the frozen direct dependency set.
- pnpm 11 rejected the removed `onlyBuiltDependencies` setting during the first install. The project now uses `pnpm-workspace.yaml#allowBuilds` and permits only `esbuild`.
- The successful install executed only `esbuild@0.25.12`'s reviewed postinstall. `node_modules/.modules.yaml` records `pendingBuilds: []` and `allowBuilds.esbuild: true`.
- `corepack pnpm install --frozen-lockfile`: exit 0 and reported `Already up to date`.
- `corepack pnpm list --depth 0`: exit 0; all ten direct packages match the exact versions in `package.json`.

## Electron development smoke

- Electron was absent from `node_modules/electron/dist` immediately after package installation, confirming that package installation alone did not install the runtime.
- The first direct `electron-vite dev` attempt exposed its Electron 43 bootstrap incompatibility: electron-vite reads `path.txt` before Electron's CLI download path. The `dev` script now runs `electron --version` before `electron-vite dev`.
- Node's direct connection to GitHub timed out (`UND_ERR_CONNECT_TIMEOUT`). A one-command `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/` fallback downloaded Electron 43.1.1; Electron's bundled checksums remained enabled.
- Final `corepack pnpm dev`: exit 0 after a normal window-close request. Evidence observed before closing: Electron `v43.1.1`, renderer dev server at `http://localhost:5173`, responding window titled `Lattice`, non-zero window handle, and a sandboxed renderer process.
- The corrected CommonJS preload artifact is `out/preload/index.cjs`, and the main process references that exact path.

## Build, types, lockfile, and artifact checks

- `corepack pnpm build` after the final preload fix: two consecutive runs, both exit 0.
- Node-side and renderer-side strict TypeScript checks using `tsc.cmd`: both exit 0.
- `node scripts/verify-planning-docs.mjs`: exit 0; 50 required files, 77 tasks, 85 automated cases, 13 manual cases, 83 requirements, and 36 compatibility items.
- Root lockfile check: exactly `pnpm-lock.yaml`; no `package-lock.json`, `npm-shrinkwrap.json`, or `yarn.lock`.
- Artifact SHA-256:
  - `out/main/index.js`: `06D980FA3E2B7115CA49DE68E7F799300A05F9D34894D5D6DF8D2DCDE7BB1ADA`
  - `out/preload/index.cjs`: `77F5EEC38C5EF075E11892244CCF9E249D82937E07401398DF98319568775A17`
  - `out/renderer/index.html`: `A57AE88E2B367A43F27CF8491565691BF725D4C73054AFD1CBD4427A4B2A73C2`

## TC-M0-001 / ENV-M0-A

- Verification path: `C:\Users\24299\AppData\Local\Temp\Lattice M0 中文 空格 20260718-2039`.
- The temporary verification workspace and its independent store were removed after evidence collection.
- The path used a project-local, initially empty `.pnpm-store`, so no global pnpm package cache was reused.
- First frozen install: exit 0, downloaded 84 packages, executed only the approved esbuild postinstall.
- Second frozen install: exit 0, `Already up to date`.
- Two development smoke runs produced responding `Lattice` windows.
- Two production builds exited 0.
- The second build produced the same three artifact hashes listed above, demonstrating repeatability in this environment.
- The temporary project contains only `pnpm-lock.yaml` and no npm/yarn lockfile; `pendingBuilds` is empty.

M0-T01 used the documented bootstrap exception, so `pnpm check` was not created or run. M0-T02 must automate TC-M0-001 and establish the full quality command chain.
