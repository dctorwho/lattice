# `<TASK-ID>`：任务标题

- 状态：以 `tasks/state.json` 为准
- 依赖：`<TASK-ID>`
- 需求：`DOC-*`、`EDT-*` 等
- 兼容：`COMP-*`
- 风险：`R-*`
- 测试用例：对应 `docs/test-cases/Mx-*.md` 中的 `TC-*`；人工门禁任务同时列出 `MAN-*`

## 目标

一句话描述用户可观察的完成结果。

## 非目标

- 明确不在本任务实现的相邻能力。

## 交付

- 代码、契约、夹具、文档和状态的具体变化。

## 自动验证

```powershell
pnpm <targeted-test>
pnpm check
```

列出测试 ID 和断言，不以“测试通过”笼统代替。

## 人工验证

无则写“无”；有则写可重复步骤和预期结果。

## 完成条件

- 自动验证全部通过。
- 没有违反 `AGENTS.md` 和数据不变量。
- 文档、兼容矩阵和状态同步。
